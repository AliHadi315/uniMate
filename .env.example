# Copy this file to `.env` (same folder) and fill in your key.
# `.env` is git-ignored, but the build declares it as an asset in pubspec.yaml,
# so the file must exist — an empty value is fine, the AI tab just reports it.
#
# Get a key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
